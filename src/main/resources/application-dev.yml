spring:
  datasource:
    url: jdbc:mysql://localhost:3307/petfarewell?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: petowner1
    password: 0826PET!

    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 3004

kakao:
    client-id: ${KAKAO_REST_API_KEY}
    redirect-uri: ${KAKAO_REDIRECT_URI}

jwt:
    secret: ${JWT_SECRET_KEY}
    access-token-validity: ${JWT_ACCESS_TOKEN_VALIDITY}
    refresh-token-validity: ${JWT_REFRESH_TOKEN_VALIDITY}

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    s3:
      bucket: petfarewell-bucket
    region:
      static: ${AWS_REGION}
    stack:
      auto: false
